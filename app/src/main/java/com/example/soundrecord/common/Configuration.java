package com.example.soundrecord.common;

public class Configuration {
    // 确定播放状态
    public static boolean IS_PLAY = false;
    // 确定是否可以点击保存状态、 默认开启录音后就不再显示目录可以直接保存
    public static boolean IS_SAVE_STATE = false;
}
